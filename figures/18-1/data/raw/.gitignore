# Full public replication archive retained locally (106 MiB), exceeds GitHub's
# file limit and contains unrelated survey data. URL/hash/member inventory and
# exact figure-specific GPH are committed instead.
EasterlinParadox.zip
